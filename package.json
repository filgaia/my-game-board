{
  "name": "my-game-board",
  "private": true,
  "scripts": {
    "audit": "lerna-audit",
    "bootstrap": "lerna bootstrap --hoist",
    "start": "lerna run start --stream",
    "clean": "lerna clean -y"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "lerna-audit": "^1.3.3"
  }
}
